schema: overlay-invariants/v1
items:
  - id: INV-metrics-null-not-zero
    text: A metric the platform did not return stays null from adapter to pool, filter and sort. It is never defaulted to 0 or guessed, and a null is never ranked or filtered as if it were 0.
    function_ids:
      - INV-01
    span: local
