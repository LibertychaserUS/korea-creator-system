# 已知问题与后续计划

## 当前状态
- 当前版本：`v8a-dashboard`
- 当前 UI 已完成深色情报仪表盘基础重构与三页核心演示。

## 已解决问题
- 已修复旧版页面视觉松散、结构单薄的问题。
- 已修复 `/favicon.ico` 404。
- 已移除页面中的旧开发文案，如 `Stage 7 Frozen`、`v3-stage3`。
- 已补齐概览图表、详情抽屉、AI复核独立页与三语基础切换。

## 仍然存在的问题
1. 人工复核、邮件触达、数据与导出、规则设置尚未形成独立完整页面，本轮仅保留导航入口与空状态。
2. 世界地图依赖前端 GeoJSON 加载；已实现失败降级，但仍建议后续补充本地地图资源与失败场景测试。
3. 当前 AI 真实复核仅 10 条，剩余 40 条仍为 fallback 缓存。
4. 数据存储仍为 CSV / JSON，适合 Demo，不适合复杂协作与并发写入。
5. 当前系统仍为单平台 Demo，未接入多平台、多国家完整数据源。

## 第八阶段 B 建议范围
1. 人工复核独立页
   - 独立列表
   - 待确认池
   - 已复核筛选
   - 复核效率看板
2. 邮件触达页
   - 模板展示
   - 发送前检查
   - 外部渠道待接入提示
3. 数据与导出页
   - 导出记录
   - 文件类型选择
   - 命名规则展示
4. 规则设置页
   - 只读展示当前评分权重与扣分口径
   - 后续再补可编辑能力

## 中长期计划
1. 扩展 Top50 全量真实 DeepSeek 复核。
2. 接入更多原始 Excel 批次。
3. 引入 SQLite 持久化，提高人工标注与缓存管理稳定性。
4. 增加多平台能力，如 TikTok、Instagram、YouTube 等国际达人数据接入。
5. 保留小红书、TikTok 以及国际数据统一视图的产品化方向，但当前未接入。

## 明确未接入项
- 多平台采集：未接入
- 小红书主页抓取：未接入
- 邮件自动发送：未接入
- 国际数据自动同步：未接入
- 规则在线编辑并持久生效：未接入

以上项目均不得视为当前已完成能力。

## 2026-06-18 第八阶段 A.1 更新

### 已解决
- 三语页面标题、导航、工具栏、筛选项、状态标签、图表标题、详情抽屉说明不完整的问题已修复。
- `fallback` 被错误计入真实 AI 决策比例与冲突率的问题已修复。
- `A/S + cautious` 被标红为强冲突的问题已修复，现归类为 `soft_divergence`。
- AI 抽屉自然语言内容支持英文、韩文翻译缓存，避免切换语言时重复跑完整 AI 复核。

### 仍然存在
1. 第八阶段 B 的独立页面尚未开发：
   - 人工复核独立页
   - 邮件触达页
   - 数据与导出页
   - 规则设置页
2. 当前韩文与英文地区映射基于已配置地区字典；若未来新增新地区值，需要继续补字典。
3. 翻译缓存当前只在访问过的达人/语言组合上生成，未做全量预热。
4. 真实 AI success 结果当前仍为 10 条，fallback 仍为 40 条。

### 第八阶段 B 建议
1. 人工复核独立页：
   - 单独待确认池
   - 复核进度统计
   - 批量状态筛选
2. 邮件触达页：
   - 邮件模板预览
   - 触达状态占位
   - 外部渠道“待接入”提示
3. 数据与导出页：
   - 导出历史记录
   - 导出文件类型与命名规则说明
4. 规则设置页：
   - 先做只读展示
   - 后续再做可编辑与回写

### 中长期保留方向
- 多平台接入：小红书、TikTok、Instagram、YouTube
- 国际数据接入与统一视图
- SQLite 持久化增强
- Top50 全量真实 DeepSeek 复核扩充

## 2026-06-19 第八阶段 A.2 更新

### 已解决
- 韩文和英文模式仍混入中文的问题已修复：
  - 六维评分维度标签（分层筛选、关键词组合、韩国品牌等）现已正确本地化
  - 概览页数据说明现已正确本地化
  - 评分依据抽屉中的维度标签现已正确本地化
  - 雷达图维度标签现已正确本地化
  - 规则评分说明现已正确本地化
- 后端硬编码中文改为返回结构化数据，前端统一使用 `t()` 函数本地化
- 翻译缓存内容正确，无需清理

### 仍然存在
1. 第八阶段 B 的独立页面尚未开发
2. 当前韩文与英文地区映射基于已配置地区字典；若未来新增新地区值，需要继续补字典
3. 翻译缓存当前只在访问过的达人/语言组合上生成，未做全量预热
4. 真实 AI success 结果当前仍为 10 条，fallback 仍为 40 条

### 豁免保留原文范围（不翻译）
- 达人昵称
- 小红书号
- 原始抓取关键词
- 用户手写的人工备注
