# 页面说明

四个 Nuxt 端都 extend `libs/panel`：同一套壳（侧栏、顶栏、语言 / 主题 / 同意条）、同一登录页、同一 composables（`useApi` `useSession` `useMetrics` `useFormat` `useTheme` `useConsent`）。路由带语言前缀 `/{zh-CN|en|ko}/…`。testid 的权威在 `packages/kcs-contract/src/testids.ts`；页面级 testid 是 `screen-<letter>-<name>`。

界面上的每个数字都来自 `CreatorMetrics`，展示统一走 `MetricValue`（按单位格式化 + 分位色带 + 悬停口径与"同源同量级 N 人"），量级用 `TierBadge`，健康等级用 `HealthBadge`，来源用 `SourceBadge`（fixture 时带"样例"）。

## 共用（libs/panel）

| 页面 | 路径 | 说明 |
|---|---|---|
| 登录 | `/login` | 左侧品牌叙事 + 右侧表单，`POST /__login`（TinyShip better-auth）。工作端登录落本端首页；宣传站登录按角色跳到对应工作端（尊重 `kcs_last_ws`）。testid `login-email` `login-password` `login-submit` `login-error` |
| 无权限 | `/denied` | `screen-denied` |
| 侧栏 | — | 按 `can(role, perm)` 显示工作区；`nav` 条目可带 `perm`（没有该权限就不显示，路由中间件同样按路径拦到 `/denied`）；子路径时只点亮最具体的一项；退出 `POST /__logout` |
| 顶栏 | — | 工作台切换在窄屏上可横向滑动，不撑出整页横向滚动 |

## 宣传站 marketing（7005）

单页 `apps/marketing/pages/index.vue`，公开。区块：

1. Hero：一句话 + 登录入口。
2. 数据来源：路线 A 蒲公英（官方 / TikHub / JustOneAPI）、路线 B 千瓜 / 新红；明确"不自建抓取，取不到的不编"。
3. 指标怎么读：六个指标组、量级、健康等级、分位（同源同量级）。
4. 筛选方案：替代评分；示例条件。
5. 四个端各做什么。
6. 预览表（静态，字段来自 `GET /api/metrics/fields`）。

文案见 11。

## 选人端 select（7004）

| 页面 | 路径 | testid | 说明 |
|---|---|---|---|
| 博主池 | `/` | `screen-c-pool` | **方案栏** `query-bar`：分「我的方案」「团队方案」两排（`query-shelf-mine` / `query-shelf-team`），每个方案一个 chip `query-tab-<id>`，只有自己看得到的带锁；新建 / 编辑；快捷筛选 `filter-tier` `filter-health`；搜索昵称 / 小红书号 `pool-search`，可点 `pool-search-save` 把搜索词存进当前方案（方案里存的搜索显示为 `query-saved-search`）；排序 `sort-key`；服务费 `service-fee`（`service-fee-0` / `-10` / `-20`，默认不含）。表格 `table-pool` 列 = 方案 `columns`，每格 `MetricValue`（分位色带、悬停写明和谁比：本库同类 N 人、粉丝范围，或小红书平台排位）；快照超 60 天的行标 `row-stale` 且不显示分位；行首 `TierBadge` `HealthBadge` `SourceBadge`；高亮 `flags`。移动端为卡片，优先显示排序键。每页 50 位、底部翻页 `pool-pager`：上一页 / 下一页走游标，跳页走页码；搜索与筛选都在服务端做，条件一改回到第一页；游标失效（服务重启换了密钥等）时自动改按页码重取同一页，已勾选的博主跨页保留；计数是全部匹配数。**方案编辑器**：名称、可见范围 `query-visibility`（`query-visibility-private` 我的 / `-team` 团队，只有作者能改）；范围：分类 `query-categories`（`query-category-<slug>`）、是否合作过 `query-collab`（`query-collab-any` / `-yes` / `-no`）、合作次数 `query-collab-min` / `-max`、搜索词 `query-search`；条件行（≥ / ≤ / 分位 ≥，填绝对值时下方 `reference-chips` 给本库同类 25 / 50 / 75 分位可点选，不足 30 人显示 `reference-none`）；条件组 `query-add-group-any`（满足其一）/ `query-add-group-exclude`（排除），每组 `query-group-<n>`、组内加条件 `query-group-<n>-add`；排序；高亮 `query-highlight-<n>`（按值或按分位，可限定来源 `query-highlight-source-<n>`，健康门槛 `query-health-gate`）；显示列。保存带版本号：别人先保存了会提示 `query-notice` 并给出最新内容，不会覆盖。**修改记录** `query-revisions`：每个版本 `query-revision-<v>`（谁、何时、做了什么），`query-revision-use-<v>` 套用那一版（存成新版本）。删除 = 收起，出现「撤销」`query-undo-delete`。 |
| 博主详情 | `/creators/:id` | `screen-c-creator` | 身份条（量级、健康、来源、`creator-cohort` 和谁比：来源 × 时间窗 × 内容形式、N 人）+ 3 个头条指标（`headline-percentile` 排位、`headline-reference` 本库同类 25 / 50 / 75 参考值）；快照超 60 天时 `creator-stale` 提示「数据已超过 60 天，暂不排位」；**发布时与最新** `metric-compare`：筛选与排序用发布时的数字，旁边列出之后抓到的最新数字与变化；**趋势卡** `creator-trend`：30 / 90 天切换，粉丝 / 阅读中位数 / 互动率 / CPE 四条迷你折线与首末变化，展开看全部快照表；六组指标卡（每行分位条 + 值，派生值标 ƒ）；受众画像；**数据来源卡** `creator-sources`：每个来源的 externalId 与最后抓取时间；合作品牌；供应商指数。 |
| 项目列表 | `/projects` | `screen-c-projects` | `table-projects`，`btn-create-project` |
| 新建项目 | `/projects/new` | `screen-c-project-new` | |
| 项目详情 | `/projects/:id` | `screen-c-project-board` | KPI（人数、CPE 中位、互动率中位、健康分布）；分派表（量级、健康、CPE、互动率；已下架的博主标「已下架」）；`btn-assign` 从池中加人；**导出表格** `btn-export-project`（按当前界面语言出表头，Excel 直接打开）；有 `select.assign` 时每行 `btn-remove-assignment` → 确认 `btn-remove-assignment-confirm` 移出项目。 |

## 运营端 ops（7002）

| 页面 | 路径 | testid | 说明 |
|---|---|---|---|
| 概览 | `/` | `screen-a-home` | 待审核（`tile-pending`）/ 需复核 / 已发布 / 已下架计数，点数字进列表对应标签；最近任务表（`SourceBadge`、状态）。 |
| 博主列表 | `/creators` | `screen-a-creators` | 标签 待审核 / 已发布 / 已下架（`ops-creator-tab-*`，数字随搜索变）；搜索昵称 / 小红书号 `ops-creator-search`；来源筛选 `ops-creator-source`（含手动录入）；每页 20 条 `ops-creators-pager`（服务端分页，标签数字由服务端按当前搜索 / 来源统计）；条件写进地址栏，可分享。桌面表 `table-ops-creators` / 行 `row-ops-creator`，手机卡片 `card-ops-creator`。点一行进详情。 |
| 博主详情 | `/creators/:id` | `screen-a-creator` | 顶部：返回列表、平台原始信息 `btn-raw`（抽屉 `sheet-raw`，按来源与时间列出每次抓到的记录 `raw-record`，可展开原样内容）、按阶段显示 **通过并发布** `btn-publish` / **下架** `btn-unpublish` / **重新发布** `btn-republish`，都要确认（`*-confirm`）。身份条 `creator-status`（`data-status` / `data-stage`）、`creator-key`；**发布时与最新** `metric-compare`；趋势 `creator-trend`（30 / 90 天）；资料编辑 `form-creator-edit`（显示名、粉丝 / 粉丝未知、地域、内容方向、分类、备注，保存 `btn-save-creator-edit`）；数据来源 `creator-sources`。 |
| 新建博主 | `/creators/new` | `screen-a-creator-form` | 手工录入；保存后跳到详情页审核、发布。没有指标的博主在池里显示"—"。`btn-create-creator` |
| 数据源 | `/sources` | `screen-a-sources` | **适配器卡**（`adapter-<source>`：路线、支持参数、提供指标、凭证 env 是否配置、可选 env）；**抓取参数表单** `fetch-form`（来源 `fetch-source`、窗口 30/90、关键词 `fetch-keyword`、类目、地域、条数、粉丝区间、报价区间、健康；不支持的参数置灰；未配置凭证时显示 `fetch-fixture-note`）；提交 `fetch-run` → 202 显示"已加入队列 + 任务号"，或 sync 结果 `fetch-result`；**任务表** `table-fetch-jobs`：来源 / 参数 / 写入 / 失败 / 状态（下方"第 N 页 · 调用 M 次"、`partial` 显示续跑时间、`failed` 显示错误）/ 操作（`job-retry` `job-cancel`）。有活动任务时每 3 秒刷新。 |
| 分类 | | | 三语名、启用、前台可见。 |
| 批量导入 | | | Excel / CSV 上传，走同一张 `ingest_jobs`。 |

## 运维端 dev（7003）

| 页面 | 路径 | testid | 说明 |
|---|---|---|---|
| 账号 | `/accounts` | `screen-b-accounts` | 只有平台管理员（`admin.users`）看得到、打得开。列表 `table-accounts` / 手机卡片 `card-account`：账号、角色下拉 `account-role`（改完即存）、最近登录、状态 `account-status`、停用 `btn-disable-account` / 恢复 `btn-enable-account`（确认 `btn-account-confirm`）。新建 `btn-create-account` → 抽屉表单 `form-create-account`（邮箱、名字、初始密码、角色）。页面写明：改角色、停用最多 10 秒后生效。自己那一行不能改角色、不能停用。 |
| 健康 | `/` | `screen-b-health` | DB 状态；任务状态分布条；任务表 `table-jobs`（来源徽标、写入 / 失败、状态；`failed` / `partial` 可 `btn-retry-job`，需 `dev.retry`）；流水线：每个来源今日调用 / 配额；审计流；三语键数。 |

## 状态与色调

`StatusBadge` 统一翻译与配色：`ok` 绿、`running` 蓝、`queued` / `partial` 琥珀、`failed` 红；博主 `draft` / `review` / `ready` / `released` 同理。分位色带：`top10` 主色加粗、`top25` 主色、`upper` 正常、`lower` 弱化、`bottom` 琥珀；只在 top25 以上和 bottom 点亮小圆点。同类只有 10–29 人时是三档：`front` 同 top25、`middle` 正常、`back` 同 lower（弱化）；不足 10 人不上色。
