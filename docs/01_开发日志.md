# 开发日志

## 2026-09-12 产品硬约束：绿地三语提前到第一刀

- 用户判定 tinyship 应用必须是 `zh-CN` / `en` / `ko`，不是计划书 V1.1 / Later。
- SSOT：`docs/product/TINYSHIP-REBUILD.md` §5.1。实现抄本机 TinyShip/Ascendia（`libs/i18n` + `@nuxtjs/i18n` 前缀 + `NEXT_LOCALE`），扩 `ko`。
- 工单：`KCS-TS-01I`；`KCS-TS-01` 依赖它。`KCS-LATER-01` 取消。
- 本轮只改 `docs/product/` 与 `docs/pm/`，未脚手架、未搬 archive 字典。

## 2026-06-16
- 初始化项目骨架目录。
- 建立 `docs/` 基础文档。
- 建立 `config/` 配置骨架。
- 实现 `0201` 读取、字段统计、去重统计、预览输出。

## 2026-06-17
- 实现六维规则评分引擎、最终分、等级、排名。
- 输出 Top10 / Top50 / 全量 CSV 与 Excel。
- 完成 FastAPI 单页 Demo 基础页。
- 接入 DeepSeek AI复核与 fallback 策略。
- 增加人工标注、演示流程、测试验收文档。

## 2026-06-18 第八阶段 A
- 开发目标：将现有单页界面重构为深色、简约、宽松的“全球达人情报系统”，先完成公共框架、概览、达人库、AI复核三页与中英韩切换基础。
- 主要修改文件：
  - `app/web.py`
  - `templates/index.html`
  - `static/styles.css`
  - `static/app.js`
  - `static/favicon.svg`
  - `docs/00_项目总览.md`
  - `docs/05_接口说明.md`
  - `docs/06_页面说明.md`
  - `docs/07_测试与验收清单.md`
  - `docs/08_已知问题与后续计划.md`
  - `docs/09_Agent接力记录.md`
  - `docs/10_演示流程.md`
- 实现内容：
  - 新增固定左侧导航、顶部工具栏、深色视觉体系。
  - 完成概览、达人库、AI复核三页切换。
  - 新增右侧达人详情抽屉，包含概览、评分依据、AI复核、人工复核四个标签页。
  - 新增 ECharts 图表：Top10 最终分、等级分布、关键词 Top10、地区分布、AI状态图。
  - 统一真实数据来源，禁止写死参考图示例数字。
  - 增加 `GET /api/dashboard`、`GET /api/filter-options`、`GET /api/ai/reviews`。
  - 保持原有下载、AI生成、人工标注接口兼容。
  - 新增前端三语字典：`zh-CN` / `en` / `ko`，并保存到 `localStorage`。
  - 新增 favicon，修复 `/favicon.ico` 404。
- 测试结果：
  - 主页面、`/docs`、`/favicon.ico`、下载接口均返回 200。
  - 真实 1725 人数据读取正常。
  - Top10 / Top50 / 全量列表正常。
  - AI复核页读取 50 条 AI 数据，其中 10 条 success、40 条 fallback。
  - 人工标注保存接口已验证；测试后恢复原缓存文件内容。
  - 已完成 1366×768 与 1920×1080 浏览器截图检查。
- 遗留问题：
  - 人工复核、邮件触达、数据与导出、规则设置尚未做成独立完整页面，当前为“下一阶段完善”空状态。
  - 世界地图采用前端 GeoJSON 动态加载逻辑；若失败会降级为地区排行图，已实现但本轮未单独构造失败场景回归。

## 2026-06-18 第八阶段 A.1
- 开发目标：修复三语不完整、补齐 AI 术语说明、统一冲突口径、增加 AI 结果翻译缓存，并保持现有评分、排序、DeepSeek 主复核逻辑兼容。
- 主要修改文件：
  - `app/web.py`
  - `services/ai_recommendation.py`
  - `templates/index.html`
  - `static/app.js`
  - `docs/01_开发日志.md`
  - `docs/04_AI提示词与DeepSeek说明.md`
  - `docs/05_接口说明.md`
  - `docs/06_页面说明.md`
  - `docs/07_测试与验收清单.md`
  - `docs/08_已知问题与后续计划.md`
  - `docs/09_Agent接力记录.md`
  - `docs/10_演示流程.md`
- 实现内容：
  - 补齐 `templates/index.html` 的页面标题、品牌标题、副标题、项目名、搜索框占位、抽屉关闭按钮等硬编码文本。
  - 前端三语字典覆盖概览、达人库、AI复核、详情抽屉、图表标题、tooltip、状态、术语说明、按钮、分页、日期格式与地区名称映射。
  - 新增 `?lang=zh-CN|en|ko` 语言入口，并与 `localStorage` 同步，便于演示和截图复核。
  - 将规则评分与 AI 结论冲突判断集中到后端公共函数 `classify_ai_alignment(...)`。
  - 冲突口径修正为：
    - `hard_conflict`：`S/A + reject`、`D + recommend`
    - `soft_divergence`：`S/A + cautious` 及其他需要人工确认但不属强冲突的真实 success 结果
    - `consistent`：`S/A + recommend`、`B/C + cautious`、`D + reject`
    - `unknown`：`fallback / pending / failed / 缺少有效 AI 决策`
  - `fallback` 不再计入真实 AI 决策比例，也不计入强冲突率。
  - 新增 AI 翻译缓存：按 `creator_key + 原文哈希 + language` 存入 `data/cache/ai_translation_cache.json`。
  - 新增 `POST /api/ai/translate`，真实 success 结果支持“分析一次，翻译展示”；fallback 直接返回本地化模板，不调用 AI 翻译。
- 实测结果：
  - 真实达人总数 `1725`
  - Top50 `50`
  - 真实 DeepSeek success `10`
  - fallback `40`
  - `hard_conflict = 0`
  - `soft_divergence = 10`
  - `consistent = 0`
  - 真实决策分母 `10`
  - 决策分布：`recommend 0%`、`cautious 100%`、`reject 0%`
  - 英文、韩文翻译接口命中缓存成功；fallback 本地化模板返回成功
  - `/`, `/api/dashboard`, `/api/ai/reviews`, `/api/ai/translate`, `/favicon.ico`, `/docs` 均返回 200
  - 已完成中英韩页面与 AI 详情抽屉截图
- 口径修正结论：
  - 修正前：旧页面把 `Top50` 中大量 `cautious / fallback` 都当作冲突展示，旧统计口径可见冲突数为 `50`
  - 修正后：强冲突数为 `0`，谨慎复核数为 `10`，fallback `40` 归入 `unknown`，不参与真实冲突率与真实决策比例
- 遗留问题：
  - 本轮未开发第八阶段 B 的四个独立页面。
  - 翻译依赖现有 DeepSeek 兼容客户端；若无 API Key 或翻译失败，系统会回退原文展示并标记"暂未翻译"。

## 2026-06-19 第八阶段 A.2：三语残留修复
- 开发目标：修复韩文和英文模式仍混入中文的问题，排查完整的数据渲染链路，确保非豁免中文残留为0。
- 主要修改文件：
  - `app/web.py`
  - `static/app.js`
  - `docs/01_开发日志.md`
  - `docs/04_AI提示词与DeepSeek说明.md`
  - `docs/05_接口说明.md`
  - `docs/06_页面说明.md`
  - `docs/07_测试与验收清单.md`
  - `docs/08_已知问题与后续计划.md`
  - `docs/09_Agent接力记录.md`
  - `docs/10_演示流程.md`
- 问题根因：
  1. `web.py` 中 `DIMENSION_LABELS` 和 `WEIGHT_LABELS` 硬编码中文标签，直接返回给前端
  2. `web.py` 中 `data_source_note` 和 `ai_source_note` 硬编码中文说明文本
  3. `web.py` 中 `rule_note` 硬编码中文说明
  4. 前端部分位置直接使用后端返回的中文 `label` 字段而非调用 `t()` 函数
- 修复内容：
  1. 将 `DIMENSION_LABELS` 和 `WEIGHT_LABELS` 改为返回翻译key（如 `dimension.layered_match`）
  2. 将 `data_source_note` 和 `ai_source_note` 改为返回结构化数据（key + vars），前端使用 `t()` 函数本地化
  3. 将 `rule_note` 改为返回翻译key `drawer.ruleNote`
  4. 前端添加缺失的翻译key：`note.data_source`, `note.ai_source`, `note.no_data`, `note.no_ai_data`
  5. 修复 `renderDrawerScoring` 函数，确保维度标签使用 `t()` 函数本地化
  6. 修复 `renderDrawerCharts` 函数，确保雷达图维度标签本地化
  7. 修复概览页数据说明显示，使用新的key和变量
- 新增翻译key：
  - 中文：`note.data_source`, `note.ai_source`, `note.no_data`, `note.no_ai_data`
  - 英文：对应英文版本
  - 韩文：对应韩文版本
- 缓存处理：
  - 翻译缓存 `ai_translation_cache.json` 内容正确，韩文翻译确实是韩文内容
  - 无需清理错误缓存
- 测试结果：
  - 服务启动正常，API返回200
  - 韩文模式预览已打开
  - 评分、排名、AI结论未修改
- 豁免保留原文范围：
  - 达人昵称
  - 小红书号
  - 原始抓取关键词
  - 用户手写的人工备注
- 遗留问题：
  - 本轮未开发第八阶段 B 的四个独立页面
  - 翻译依赖现有 DeepSeek 兼容客户端；若无 API Key 或翻译失败，系统会回退原文展示

## 2026-09-12 旧 Python Demo 重新归档
- 开发目标：纠正 `4b406a0` 把 v2 Demo 迁回仓库根的状态，按产品文档把旧 FastAPI / pandas 代码冻回 `archive/`。
- 主要修改文件：
  - `archive/v8a-dashboard/`（由根目录 `app/`、`services/`、`config/` 等迁入）
  - `archive/README.md`（墓碑）
  - `README.md`
  - `AGENTS.md`
  - `.gitignore`
- 实现内容：
  - 仓库根不再保留可施工的 `app/`。
  - 墓碑写明冻结路径、历史入口、对照文件与禁止事项。
  - 根 README 指向 `docs/` 与 archive，不再把 `uvicorn app.web:app` 写成当前运行方式。
- 测试结果：
  - `git ls-files` 确认根目录无 `app/`、`services/`、`requirements.txt`。
  - `docs/` 未搬迁。
  - 未启动 archive 内进程（冻结物不是运行入口）。
- 后续：
  - 该归档提交（`f31c882`）与四端实现线 `feature/kcs-four-apps-completion` 各自独立做了同一件事；合并时以四端线的 `README.md` / `AGENTS.md` / `archive/README.md` / `.gitignore` 为准，`main` 只贡献本条日志。
  - `TINYSHIP-REBUILD.md`、`TINYSHIP-PIN.md` 已在四端线落地；实现状态与下一步见 `docs/HANDOFF.md`。

## 2026-09-13 四端前端设计重做与截图验收
- 开发目标：按 `docs/HANDOFF.md` 接力，清掉旧 Python 代码后把四端前端「做精做好」——不是修补，而是对工作台外壳与全部页面做一次完整的设计升级，并用截图验收。
- 分支：`cursor/land-four-apps-on-main-d3aa`（PR #9，基于 main）。
- 主要修改文件：
  - 外壳：`libs/panel/components/AppSidebar.vue`、`libs/panel/layouts/default.vue`、`libs/panel/components/PanelPage.vue`、`libs/panel/components/ui/sidebar/SidebarProvider.vue`、`config.ts`、`libs/panel/assets/css/main.css`
  - 通用组件：`libs/panel/components/{KpiTile,TableCard,StatusBadge,GradeBadge,EmptyState}.vue`、`libs/panel/composables/{useFormat,useCurrency}.ts`
  - 页面：`apps/select/pages/**`、`apps/ops/pages/**`、`apps/dev/pages/index.vue`、`apps/marketing/pages/index.vue`、`libs/panel/pages/login.vue`、`libs/panel/layouts/auth.vue`
  - 文案：`libs/i18n/locales/{zh-CN,en,ko}.ts`（`kcs.roles`、`kcs.landing`、`kcs.panel.*` 新词条）
  - API：`apps/api/src/migrate.ts`（补列）、`apps/api/src/app.ts`（CORS 多来源、录入保存小红书号、概览返回批次名）
- 实现内容：
  - 宣传页从「一段话 + 三张卡」扩成完整落地页：锚点导航、静态达人库预览、三工作台卡、四步流程、六维评分权重 + S/A/B/C 分级 + 规则版本、主张、CTA、页脚。
  - 登录页桌面双栏（品牌叙事 + 表单）。
  - 选人端达人库：筛选/分段排序/表格/骨架屏/空态，手机切卡片列表；项目列表、新建（表单 + 预览）、详情（KPI + 名单）。
  - 录入端：KPI + 最近批次 + 清洗率 + 快捷入口；入档表单分区、头像自定义上传按钮、实时预览、两步发布。
  - 监测端：KPI + 任务分布 + 最近任务（失败原因、按权限重试）。
  - 写成功但有失败行的任务在界面上显示「部分完成」。
- 修掉的坑：
  - Tailwind v4 不扫描 Vite root 之外的 `libs/panel` → `@source` 显式登记。
  - scoped 样式 `:global(.dark) .x` 编译成裸 `.dark` → 改 `:global(.dark .x)`，暗色背景恢复。
  - `pages/projects.vue` 与 `pages/projects/[id].vue` 并存导致子路由渲染父页 → 移到 `projects/index.vue`。
  - 手机端 `useMediaQuery` SSR 水合不一致 → `SidebarProvider` 挂载后再取值。
  - 新增 auto-import composable 后 build typecheck 报 TS2304 → 先 `npx nuxi prepare`。
- 测试结果：
  - 四端 `pnpm build` 全部通过。
  - Playwright 生产构建截图 36 张（4 端 × 亮/暗 × 桌面/手机），控制台错误、HTTP≥400、横向溢出、水合警告均为 0。
  - HANDOFF §4 的 `/en` 文案 bug 未复现（10 条路径均正确）。
- 未做 / 交给后续：nuxt-app（7001）tide 主题验证；ColorSchemeSelector 精简。后端/部署收尾由 sol 子代理完成，见 `docs/HANDOFF.md` 文末。

## 2026-09-14 删除六维评分，改为「有参数的爬虫 + 转换层 + 指标筛选方案」
- 起因：用户明确否定自造的六维权重 / S-A-B-C 阈值——「本质就是一个有参数的爬虫」。调研蒲公英 / 千瓜 / 新红 / 星图 / HypeAuditor 的实际口径后重做：平台给什么字段就看什么字段，好坏只在同粉丝量级里比分位，健康等级是门不是分。
- 数据源路线：A 蒲公英 OpenAPI（官方）、B 千瓜 / 新红（第三方付费 API）。**不做 C（自建爬虫）**。
- 契约层 `packages/kcs-contract`：
  - 删 `rule-score.ts`；新增 `metrics.ts`（`CreatorMetrics` 33 个字段 + `deriveMetrics` 纯除法派生 + `tierOf` 千瓜量级 + `cohortPercentiles` 同量级分位）、`source-adapter.ts`（`SourceQuery` 抓取参数、`RawRecord`、`SourceAdapter`、解析助手）、`saved-query.ts`（`SavedQuery` 筛选方案 = 过滤 + 排序 + 高亮阈值 + 显示列，`applySavedQuery` 纯函数）。
  - `api.ts` 新增 `/api/select/queries*`、`/api/ingest/adapters|fetch|raw`、`/api/metrics/fields`。
- API `apps/api`（sol）：`adapters/{pugongying,qiangua,xinhong}.ts` 各带可编辑 `FIELD_MAP`；无凭证时返回 fixture 并标 `sourceMode: 'fixture'`；原始 JSON 存 `creator_raw`；`creators.metrics/metrics_locked` 列；发布时锁定指标快照；pool / 详情返回 `tier` / `metrics` / `percentiles`；筛选方案 CRUD + 运行；种子改为三源 fixture 24 人 + 2 个方案。
- 前端：选人端达人库改为「方案栏（已保存方案 / 量级 / 健康等级 / 排序 / 搜索）+ 方案编辑器（指标条件、高亮阈值、显示列、保存/另存/删除）+ 列由方案决定的指标表」；新增 `/creators/[id]` 六组指标面板（分位条、受众、合作品牌、平台指数）；运营端新增 `/sources`（三适配器卡 + 抓取参数表单 + 抓取记录）；监测端任务标数据源与模式；宣传页评分块改为「平台给什么，我们就看什么」（三数据源、六组字段、同量级比、健康等级门、转换层）。新组件 `MetricValue / TierBadge / HealthBadge / SourceBadge`，composable `useMetrics`；删 `GradeBadge`。i18n 三语新增 `kcs.metric / metricHelp / metricGroup / tier / health / band / source / query / ingest`。
- 清理：移除已废弃单宿主 `apps/web`（依赖旧评分）。
- 测试：contract 25 通过；API 35 通过；黑盒 90 通过；四端 `pnpm build` 通过；Playwright E2E 6/7 通过（05 图片上传需 MinIO，本机未起）；生产构建截图 44 张，控制台错误 / HTTP≥400 / 横向溢出为 0（唯一报错是截图脚本里的旧种子项目 id）。
- 未做（当时）：真实蒲公英 / 千瓜 / 新红接口未联调（无凭证与文档），`FIELD_MAP` 路径为假定值。→ 见下一条，蒲公英已按真实字段重写。

## 2026-09-14 蒲公英按真实接口字段重写；千瓜 / 新红改为合同后配置
- 起因：用户指示「没有凭证正常，你去找文档」。找到的公开资料：TikHub（api.tikhub.io，OpenAPI 3.0 全量描述含 `get_blogger_list` 约 60 个筛选项与 34 个排序字段）、JustOneAPI（api.justoneapi.com，`solar/*` 路径的 OpenAPI 描述）、多份公开的蒲公英 `solar` 响应样例（`cooperator/user/blogger`、`kol/dataV3/dataSummary|fansSummary|notesRate`、`kol/data/{id}/fans_profile`），以及多份开源客户端对同一批字段名的引用；三者字段一致，可确认为蒲公英原样 JSON。官方开放平台文档中心（ad-market.xiaohongshu.com/docs-center）需合作方登录，未能读取。
- 千瓜官网无公开 API 文档（企业服务、商务对接）；新榜数据 API（api.newrank.cn，`Key` 头，表单 POST）接口列表在付费控制台内，公开页面只有入口。两家的接口路径与字段均需拿到合同后再填。
- 契约：`SOURCE_CREDENTIALS` 改为 蒲公英 `PGY_ACCESS_TOKEN`（必填）+ `PGY_GATEWAY / PGY_BASE_URL / PGY_BRAND_USER_ID / PGY_ENRICH`（可选）；千瓜 / 新红加 `*_BASE_URL / *_SEARCH_PATH / *_FIELD_MAP` 可选项；新增 `PGY_GATEWAYS`。`/api/ingest/adapters` 多返回 `optionalEnvVars`，运营端数据源卡片一并显示。
- `adapters/pugongying.ts` 重写：三网关（tikhub POST JSON 两层 `data.data`；justoneapi GET `token` 查询参数一层 `data`；official 复用网页 `solar` 请求体，Base URL 可配），`SourceQuery` → 各网关筛选体（粉丝区间 / 图文报价区间 / 类目 / 地域 / 剔除低活 / 页码），`externalIds` 刷新 = 资料 + 4 个数据接口合并到同一 payload；`normalizePugongying` 按真实字段名映射 31 个统一字段（表见 `docs/03`），`0` 视为未展示，`lowActive` / `isActive` 落成健康门。列表 `total` 恒 5000 是翻页上限，`nextCursor` 按整页 20 条判断。
- `adapters/qiangua.ts` / `xinhong.ts`：注释写明文档状态；默认 `FIELD_MAP` 保留供应商导出口径中文列名 + 常见别名，`fieldMapFromEnv` 支持 `*_FIELD_MAP` JSON 覆盖；新红走 `Key` 头 + 表单编码（`fetchJsonPage` 新增 `encoding: 'form'`）。
- fixture `pugongying.json` 改为真实 `kols[]` 字段形状（9 人，3 人带 dataSummary / fansSummary / notesRate / fansProfile 补全）；`payloadId` 统一识别 `userId`。
- 测试：新增 `tests/pugongying.test.ts` 8 例（字段映射 / 0 值 / 健康门 / 三网关请求体与解包 / 刷新补全 / 无 Token 回退 fixture）；API 全套 43 通过；`tsc` 通过。
- 部署：`docker-compose.yml`、`deploy/k8s/api.yaml`、`secret.example.yaml` 改为新变量；`docs/03` 新增接口表、参数映射表、字段对照表。
- 仍未做：没有任何一家的 Token，真实联调（含 JustOneAPI 数据接口的查询参数名、官方开放平台的鉴权头）要等凭证到位；千瓜 / 新红字段路径要等合同接口文档。

## 2026-09-14 结构性问题收口：TinyShip 身份、队列 worker、指标历史、同源分位、身份归并、路由拆分；文档与文案按新架构重写

- 契约：`QueryResultRow.cohort`，`applySavedQuery` 按 `source × tier` 分组算分位；`MetricSnapshot`、`CreatorSourceLink`、`INGEST_JOB_STATUSES`（queued/running/ok/partial/failed）、`IngestJobProgress`；`roleFromIdentity()`（TinyShip `user.role` → KCS Role）；`API` 删 `login/logout`，加 `poolCreatorHistory`、`ingestJobRetry`、`ingestJobCancel`。
- API（sol）：`app.ts` 拆成 `routes/*` + `http/*`；迁移改为 `migrations/000N_*.sql` + `schema_migrations`；身份走 TinyShip（`__login` 服务端 `signInEmail`，API `verifySession` 向 `AUTH_BASE_URL` introspect，30s 缓存，`users` 变 profile 镜像，`KCS_DEV_TOKENS` 仅非生产）；`ingest/worker.ts` 队列 + 令牌桶速率 + `ingest_source_usage` 日配额 + `partial` 续跑 + 重试 / 取消；`creator_metrics_history` 每次 ingest 一条快照 + `/history` 端点；`creator_sources` 按 `xhs_id` 归并。详见 02 / 04 / 05。
- 前端：`MetricValue` 悬停显示"同源同量级 N 人"；新组件 `MetricTrend`（迷你折线 + 首末变化，`better: 'low'` 反向着色）；详情页加趋势卡（30 / 90）、快照表、数据来源卡、cohort；数据源页 202 入队提示、进度"第 N 页 · 调用 M 次"、`partial` 续跑时间、重试 / 取消按钮、活动任务 3 秒轮询；运维端 `partial` 可重试；退出走 `/__logout`。
- 文案（gemini）：三语 `kcs.*` 按新架构重写，宣传页文案重写；新增 `docs/11_文案清单与草稿.md`（术语表、键级清单、理由）。
- 文档：`00 02 04 05 06 07 08 09 10 HANDOFF README AGENTS` 全部只讲当前架构；`04` 由「AI 提示词与 DeepSeek」改为「抓取流水线与队列」；旧文档（demo / devlogs / pm / product / research / screenshots / ui_reference / V2 可行性报告）移到 `docs/archive/`。
