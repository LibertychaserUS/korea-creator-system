# 归档

这里是重建之前的文档：Python / FastAPI Demo 时期的评分规则审计、DeepSeek 复核、演示脚本、V2 计划书可行性分析，以及 TinyShip 重建初期的产品边界与工单。

它们只用于考古，**不描述当前系统**。当前系统的权威说明在上一级目录的 `00`–`10` 与 `HANDOFF.md`；代码考古看 git `f31c882`。

不要把这里的口径（六维评分、等级、AI 风险复核、Excel 输入）再写回新文档。
