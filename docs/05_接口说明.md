# 接口说明

API：Hono，`apps/api`，默认 `:7100`。路径与权限的唯一权威是 `packages/kcs-contract/src/api.ts`（`API` 常量）；黑盒测试 `tests/blackbox` 与前端都从那里取路径。`GET /api/openapi.json` 输出汇总。

状态码约定：401 未登录，403 无权限，404 不存在或不可见，409 冲突，400 参数错误，200 / 201 成功，202 已入队。错误体 `{ error: { code, message } }`。

响应字段一律 camelCase（时间是 ISO 字符串）；只有原样存下的平台返回（`payload`）和任务参数（`query`）保留来源的写法。手工拼的行在契约 `responses.ts` 有类型（`IngestJobView` / `BatchView` / `OpsOverview` / `ReviewView` / `AuditLogView` / `JobSampleView` / `CategoryView` / `ProjectView`），单测 `naming.test.ts` 逐个接口检查没有下划线键。路径用契约 `API` 常量，带参数的用 `apiPath(API.projectGet, { id }, { locale })` 拼（参数自动编码、缺参直接报错）；前端不写 `'/api/...'` 字面量。

### 错误码

| HTTP | `error.code` | 何时 |
|---|---|---|
| 401 | `AUTH-LOGIN` | 没带或带了失效的会话 |
| 403 | `AUTH-DENIED` | 有会话但角色不够，或账号没有 KCS 分工 |
| 400 | `VALIDATION` | 请求体不是合法 JSON（`message: invalid_json`）；或字段不合规（`invalid_body` 等），此时带 `fields: [{ path, message }]`，如 `displayName` 为空、`followers` 为负、`categories` 里有不存在的分类 |
| 400 | `SOURCE-INVALID` | 抓取参数不合法：未登记的来源、非 30 / 90 天窗口、请求体里带临时 URL（`sourceUrl` / `url`，`message: adhoc_url_forbidden`） |
| 404 | `NOT-FOUND` | 对象不存在或对你不可见。对不存在的博主 / 分类 / 项目 / 分派做写操作也是 404，**不写审计** |
| 409 | `JOB-STATE` | 任务 / 搁置记录当前状态不允许该操作（如取消已结束的任务） |
| 409 | `CONFLICT` | 唯一性冲突：重复分派同一博主（`already_assigned`）、`creatorKey` 已存在 |
| 410 | `GONE` | 已下线的接口：`POST /api/ingest/jobs`（`message: use_ingest_fetch`） |
| 413 / 415 | `UPLOAD-TOO-LARGE` / `UPLOAD-TYPE` | 见「上传限制」 |
| 500 | `INTERNAL` | 服务端缺陷；参数问题不应落到这里。同时写一行 `http.unhandled` JSON 日志（方法、路径、错误名与脱敏后的信息） |
| 503 | `UNAVAILABLE` | 进程正在停机（收到 SIGTERM / SIGINT 后），`message: shutting_down`；换个副本重试即可 |

- 先鉴权后读体：未登录或无权限时，即使请求体是坏的也先得到 401 / 403。
- 写接口的请求体用 zod 校验（`apps/api/src/http/body.ts`），未知的附加字段忽略。
- 筛选方案校验失败时，除 `error.fields` 外仍保留 `errors: string[]`（`kcs.query.errors.*` 的键），供选人端直接翻译。
- 批量分派先逐个检查再写：其中任一博主不在池内，整批都不写（400 `VALIDATION` / `not_in_pool`）。

### 分页

列表接口统一：query `page`（从 1 起，默认 1）、`pageSize`（默认 50，上限 100，超出按 100）；非数字或越界按默认值，不报错。响应 `{ items, total, page, pageSize }`，`total` 是分页前的匹配数；翻过最后一页得到空 `items`，`total` 照旧。常量与解析在契约 `paging.ts`（`PAGE_SIZE_DEFAULT` / `PAGE_SIZE_MAX` / `parsePaging`）。分页的接口：`GET /api/select/pool`、`POST /api/select/queries/run`、`GET /api/ops/creators`、`GET /api/ingest/jobs`、`GET /api/dev/jobs`、`GET /api/ops/batches`。同值的行按 `id` 定序，翻页不重不漏。

## 认证

身份在 TinyShip（better-auth）。API 自己没有登录接口。

```
浏览器 ──POST /__login（本端 Nitro）──▶ auth.api.signInEmail ──▶ Set-Cookie better-auth.session_token
                                                              └─▶ Set-Cookie kcs_session=<token>（非 httpOnly，供前端取 Bearer）
前端 useApi ──Authorization: Bearer <token>──▶ API requireAuth
                                                └─ verifySession(token)：GET {AUTH_BASE_URL}/api/auth/get-session
                                                   → { user } → role = roleFromIdentity(user.role)
                                                     KCS 五角色原样；TinyShip `admin` → platform_admin；
                                                     其他（如管理员开通但未分工的 `user`）→ 无角色 → 处处 403 AUTH-DENIED
                                                   → 正缓存 10s（`SESSION_CACHE_MS`），未命中负缓存 5s；最多 5000 条（`SESSION_CACHE_MAX`，最久未用的先出），每分钟清一次过期；有角色才 upsert users 镜像
退出 ──POST /__logout──▶ auth.api.signOut，清两枚 cookie
```

- `AUTH_BASE_URL`：任一工作端的源站（四端共用同一 TinyShip 库，谁校验都一样）。
- 退出：TinyShip 立刻撤销；API 侧最迟 `SESSION_CACHE_MS`（默认 10s）后拒绝旧 token。
- 公开注册已关闭：`POST /api/auth/sign-up/email` 一律 400，不建账号、不发会话；社交 / 手机验证码 / 微信登录也只认已开通的账号。账号只能由管理员开通：运维端「账号」页（见下方「账号管理」），或 `pnpm db:seed:auth`。
- 没有分工的账号（开通了但没被分配 KCS 角色）：`/__login` 落 `/denied`，API 一律 403 `AUTH-DENIED`（含 `/api/auth/me`），不会被当成只读选人。
- 跳转只拼站内相对路径：`/__login` 的 `locale` 只认 `zh-CN` / `en` / `ko`，其他值（如 `/evil.example.com`）一律按 `zh-CN` 处理；宣传站交接的目标源站只取服务端配置（`KCS_*_URL`），不读请求里的地址。
- `kcs_session` 在 `NODE_ENV=production` 时带 `Secure`（`SameSite=Lax`，暂保持非 httpOnly，前端要读它拼 `Bearer`）；因此生产必须走 https（浏览器对 `http://localhost` 例外）。
- 限速：better-auth 对 `/sign-in/email` 按来源 IP 限 3 次 / 10s，超出 429 并带 `X-Retry-After`；`/__login` 表单走同一限速（被限时回 `?error=1`）。来源 IP 取 `X-Forwarded-For` 第一段；请求没带这个头（前面没有代理）时，工作端用 TCP 对端地址补上（`libs/panel/server/middleware/client-ip.ts`）——否则 better-auth 在 `NODE_ENV=production` 下拿不到 IP 会整段跳过限速。前置代理必须覆盖而不是透传客户端自带的 `X-Forwarded-For`。
- `KCS_DEV_TOKENS=1`（非生产）额外接受 `Bearer dev:<email>`，仅供本地 curl 临时用；黑盒与 E2E 都走真实邮箱登录，compose 里不再开启。

| 方法 | 路径 | 权限 | 说明 |
|---|---|---|---|
| GET | `/api/auth/me` | 登录 | `{ user: { id, email, role, displayName } }` |

## 公开

| 方法 | 路径 | 说明 |
|---|---|---|
| GET | `/api/health` | 就绪探针。真的跑一次 `select 1`（2 秒超时）：正常 200 `{ ok: true, service, db: "ok", version }`；库不通 503 `{ ok: false, db: "down", … }`；停机中 503 且带 `draining: true`。`version` 取 `KCS_VERSION`，缺省为 `apps/api/package.json` 的版本 |
| GET | `/api/health/live` | 存活探针。不碰数据库，进程能应答就 200 `{ ok: true, version }`——库挂了只摘流量，不重启进程 |
| GET | `/api/openapi.json` | |
| GET | `/api/metrics/fields` | 指标目录：`METRIC_FIELDS`、`CREATOR_TIERS`、数据源与路线，宣传页与筛选方案编辑器共用 |

## 选人端

| 方法 | 路径 | 权限 | 说明 |
|---|---|---|---|
| GET | `/api/select/pool` | select.read | 已发布（且未拉黑）博主，筛选 / 排序 / 分页都在 SQL 里、读发布快照。query（列表值逗号分隔，都可省）：`q`（昵称 / creator key / 地区 / 垂类 / 合作品牌，包含即中）、`region`（包含即中）、`brand`、`tier`、`health`、`source`、`categories`、`category`（分类或垂类）、`verticals`、`hasCollaborated=true\|false`、`collabCountMin` / `collabCountMax`、`priceMin` / `priceMax`（配 `currency`）、`<指标>Min` / `<指标>Max`（如 `cpeMax=3`、`followersMin=5000`；非数字 → 空结果）、`sort`（指标键或 `followers`，默认 `cpe`）、`dir=asc\|desc`（旧名 `order` 仍认；默认 `cpe` 升序、其余降序；空值永远排最后，同值按粉丝降序再按 `id`）、`page`、`pageSize`。每行：身份、`metrics`（发布快照）、`tier`、`cohort {source,tier,size}`（没有来源的行为 `null`，不排分位）、`percentiles`（所有有好坏方向的指标）、`health` |
| POST | `/api/select/queries/run` | select.read | body 为完整 `SavedQuery`（可未保存）；query `q`（昵称 / creator key / 小红书号，包含即中）、`page`、`pageSize`。结果与契约 `applySavedQuery` 对全池求值逐行一致（单测 `pool-sql.test.ts` 对拍）：没有来源的行自成一组排分位；只给列、筛选、高亮涉及的指标算分位；`percentileGte` 在分页前按全池分位筛。排序：方案的 `sort`（空值最后）→ CPE 升序 → 粉丝降序 → `id`。每行 `QueryResultRow` + `health`：`metrics`、`tier`、`cohort`、`percentiles`、`flags` |
| GET / POST | `/api/select/queries` | read / write | 组织内筛选方案列表 / 新建（`validateSavedQuery` 校验，400 带字段错误） |
| GET / PATCH / DELETE | `/api/select/queries/:id` | read / write | PATCH 自动 `version + 1` |
| GET | `/api/select/creators/:id` | select.read | 详情（分位只对同组做一次计数，不加载整池）：身份、`metrics`、`percentiles`、`cohort`、`sources: CreatorSourceLink[]`、`sourceMode`、`metricsFetchedAt`、`metricsLocked` |
| GET | `/api/select/creators/:id/history` | select.read | `?window=30&limit=60` → `{ snapshots: MetricSnapshot[] }`，按 `fetchedAt` 升序，同日同源去重 |
| GET / POST | `/api/select/projects` | read / write | GET 行：`ProjectView`（`id`、`name`、`note`、`status`、`createdAt`、`updatedAt`、`memberCount`）。详情 `GET /api/select/projects/:id` 同一形状再加 `assignments` |
| GET | `/api/select/projects/:id` | select.read | 项目 + 分派行（每行含 `metrics`、`tier`、`percentiles`、`cohort`） |
| POST | `/api/select/projects/:id/assignments` | select.assign | `{ creatorIds: string[] }`（至少一个）；项目不存在 404，重复 409 `CONFLICT` |
| DELETE | `/api/select/projects/:id/assignments/:creatorId` | select.assign | 项目或分派行不存在 404，不写审计 |
| GET | `/api/select/projects/:id/export` | select.read | 带 BOM 的 UTF-8 CSV（CRLF），Excel 直接打开不乱码；`?locale=zh-CN\|en\|ko` 决定表头与量级 / 来源 / 状态的写法，未知值按中文。列：博主、小红书号、来源、量级、8 个默认指标（`DEFAULT_QUERY_COLUMNS`）、状态（已分派 / 已下架）、发布日期；数字用**发布时**的快照（和博主库一致）；以 `= + - @` 开头的文本前面垫 `'`；文件名 = 项目名。表头文案在 `packages/kcs-contract/src/export-sheet.ts`，单测保证与界面三语一致 |
| GET / POST | `/api/select/shortlist` | read / write | 组织候选名单；GET 行 = 池行 + `creatorId`、`addedAt`；POST `{ creatorId }`，不在池内 404 |

## 运营端

| 方法 | 路径 | 权限 | 说明 |
|---|---|---|---|
| GET | `/api/ops/overview` | ops.read | `counts`（草稿 / 待审 / 就绪 / 已发布 / 待发布 / 已撤回）与 `recentJobs`（最近 5 个任务：`id`、`status`、`writtenCount`、`failedCount`、`batchName`、`fileName`、`createdAt`） |
| GET / POST | `/api/ops/creators` | read / write | 列表 / 手工新建。GET query：`stage=review\|released\|withdrawn`、`q`（昵称或小红书号，包含即中）、`source`（来源 id；`manual` = 没有来源）、`page`、`pageSize`；按 `updatedAt` 倒序。响应另带 `counts: { review, released, withdrawn }`——`q` / `source` 筛过、`stage` 之前的各阶段人数，标签页角标用 |
| GET / PATCH | `/api/ops/creators/:id` | read / write | 详情含 `sources`、`raw` 摘要；PATCH 可写 `metricsLocked` |
| GET | `/api/ops/creators/:id/history` | ops.read | 同选人端 history（每天每个来源一条，升序），运营详情页的趋势用；博主不存在 404 |
| POST | `/api/ops/creators/:id/publish` `/unpublish` | ops.publish | 发布到选人端 / 下架（下架时项目分派标 `pool_gone`）。已发布再 publish 返回 `refreshed:false`，不改快照 |
| GET | `/api/ops/review` | ops.read | 待审列表：`id`、`creatorId`、`displayName`、`riskLevel`、`conclusion`、`status`、`createdAt` |
| POST | `/api/ops/review/:id/pass` | ops.write | |
| GET | `/api/ops/categories` | ops.read | `slug`、`nameZh` / `nameEn` / `nameKo`、`builtin`、`enabled`、`groupName`、`frontendVisible` |
| PATCH | `/api/ops/categories/:slug` | ops.categories | 启用 / 前台可见 / 三语名 |
| GET / POST | `/api/ops/batches` | read / write | 批量导入任务（GET 分页，按提交时间倒序；行 = 任务字段 + `sourceName`）；POST 必须是 multipart 带 `file`（.xlsx，可带 `batchName`），整体不超过 20 MB（超出 413 `UPLOAD-TOO-LARGE`）；不带文件 400 `VALIDATION` / `file_required`，不开任务。每行走与抓取相同的入库（见 04「入库」） |
| POST | `/api/assets` | ops.write | 头像上传，multipart `file` + `purpose`（`avatar` / `attachment`）→ 201 `{ url, key }`。见下方「上传限制」 |
| POST | `/api/assets/presign` | ops.write | `{ purpose, contentType }`；`contentType` 只收四种图片类型，其他 415 `UPLOAD-TYPE` |
| GET | `/api/assets` `/api/assets/:key` | ops.read | 已上传对象的元数据（`key`、`url`、`contentType`、`size`） |
| GET | `/api/assets/raw/:key` | 公开 | 未配置 S3 时读回字节（`<img>` 带不了 Bearer，所以匿名）。见下方「上传限制」 |

### 上传限制

- 只收 PNG / JPEG / WebP / GIF。类型按文件头魔数判定，不信客户端声明：HTML、SVG、PDF 等即使标成 `image/png` 也是 **415** `UPLOAD-TYPE`；声明了非图片类型的真图片同样 415。
- 单个文件上限 **5 MB**，超出 **413** `UPLOAD-TOO-LARGE`；超大请求体在读完之前就被拒。先校验登录与角色：未登录 401、无权限 403，不会先返回 413。
- 存储：配置了 S3（`S3_ENDPOINT` + `S3_ACCESS_KEY`）时字节只在 S3，Postgres `assets` 只存元数据（`bytes` 为空）；未配置时字节落 Postgres，由 `/api/assets/raw/*` 读回。对象键带按真实类型得出的扩展名（`avatars/<uuid>.png`）。
- 读回 `/api/assets/raw/*`：再按字节判定一次类型，不是四种图片之一（含修复前存进来的旧行）一律 404；响应头固定 `Content-Type: <判定出的图片类型>`、`X-Content-Type-Options: nosniff`、`Content-Disposition: inline`、`Content-Security-Policy: default-src 'none'`。
- 运营端新建博主页在上传前先按类型 / 大小拦一次，失败时给三语人话提示（`kcs.panel.uploadTooLarge` / `uploadWrongType` / `uploadFailed`）。

## 数据源与抓取

| 方法 | 路径 | 权限 | 说明 |
|---|---|---|---|
| GET | `/api/ingest/adapters` | ingest.read | 每个来源：`route`、`supports[]`、`provides[]`、`configured`、`envVars[]`、`optionalEnvVars[]` |
| GET | `/api/ingest/sources` | ingest.read | `ingest_sources` 配置（速率、配额、今日已用） |
| POST | `/api/ingest/fetch` | ingest.write | body `SourceQuery`（可带 `maxPages`，默认 5）。默认 **202** `{ job }` 入队；`?sync=1` 当场跑完 **201**，直接返回 job 对象（含 `writtenCount` / `skippedDupes` / `failedCount` / `sourceMode`）。任务的 `query` 只存 `SourceQuery` 字段，其余附加字段丢弃；带 `sourceUrl` / `url` 400。**这是开抓取任务的唯一入口**：配额、限速、租约都在队列里 |
| POST | `/api/ingest/jobs` | ingest.write | **已下线，410 `GONE`**（先鉴权：未登录 401、无权限 403）。它曾绕过队列当场跑、不扣配额不限速，数据源不是适配器时还会凭空造一个博主。抓取改用 `POST /api/ingest/fetch`，文件改用 `POST /api/ops/batches` |
| GET | `/api/ingest/jobs` | ingest.read | 任务列表（分页，按提交时间倒序；`source`、`status` 可逗号多选），每条含 `IngestJobProgress`（`status`、`pagesDone`、`cursor`、`quotaUsed`、`attempts`、`nextRunAt`、`error`）与 `query`、`sourceMode` |
| GET | `/api/ingest/jobs/:id` | ingest.read | |
| GET | `/api/ingest/jobs/:id/sample` | ingest.read | 该任务写入的博主：`id`、`displayName`、`needsReview` |
| POST | `/api/ingest/jobs/:id/retry` | ingest.retry | `failed` / `partial` → `queued`，从 `cursor` 继续 |
| POST | `/api/ingest/jobs/:id/cancel` | ingest.write | `queued` / `running` → `failed`，`error = cancelled` |
| GET | `/api/ingest/daily` | ingest.read | 每日任务及最近一次：`{ items: [{ task, lastDay, lastStartedAt, lastFinishedAt, ok, error, result }] }` |
| POST | `/api/ingest/daily/:task/run` | ingest.retry | 立刻重跑当天这一次，返回 `{ task, day, ok, result?, error?, ms }`；未注册 404；写审计 |
| GET | `/api/ingest/scheduler` | ingest.read | 调度器：`lastPlan`（上次计划：`split` 刷新/发现份额与方法、`arms` 两臂的试验数/成功数/Beta 参数、各来源预算与计划的任务）、`config`（当前参数）、`due`（各来源到期待刷新人数、从未刷新人数、刷新间隔中位数） |
| GET | `/api/ingest/discovery-searches` | ingest.read | 定时发现用的保存条件 |
| POST | `/api/ingest/discovery-searches` | ingest.write | `{ name, query: SourceQuery, enabled?, maxPages?(1–100, 默认 1), everyHours?(默认 24) }` → 201；`externalIds` / `cursor` 不保存；带 URL 400；写审计 |
| PATCH | `/api/ingest/discovery-searches/:id` | ingest.write | 改 `name` / `enabled` / `maxPages` / `everyHours`；写审计 |
| GET | `/api/ingest/data-status` | ingest.read | 不带 `kind`：`{ counts: { stale, missing, republish }, config: { staleDays, missingAfter } }`；`?kind=stale\|missing\|republish` + `page`/`pageSize`：该类博主的分页列表（每项同下），其他值 400 |
| GET | `/api/ingest/data-status/:creatorId` | ingest.read | 一位博主的数据状态：`stale`、`daysSinceFetch`、`lastFetchedAt`；`platformMissing`（`{ since, confirmedAt }` 或 null）；`republishable`、`changedSincePublish`（与发布快照相比超过容差的指标）、`publishedAt`；`sources[]`（每个来源的 `lastSeenAt`、`missCount`、`missingSince`、`missingAt`）。不存在 404 |
| POST | `/api/ingest/data-status/:creatorId/missing` | ingest.write | `{ decision: 'keep' \| 'gone' }`：`keep` 清掉「平台上已找不到」并把连续未取到次数归零（回到池子），`gone` 记确认时间、继续留在池外；都不删数据。未被标记 409；写审计 `creator.platform_missing_keep` / `_gone` |
| GET | `/api/ingest/trends/:creatorId` | ingest.read | 按来源分线的趋势（契约 `CreatorTrends`）：`?window=30\|90`（默认 30）、`source=`（只要一个来源）、`limit=`（每个来源最近几份，默认 90，2–365）。`series[]` 每个来源一条（`snapshots` 为 `MetricSnapshot[]`，来源从不混线；`followerSlope` 对数粉丝线性回归：`perDay`、`growth30d`、`r2`、`points`、`spanDays`，不足 3 份或跨度不足 7 天为 null）；`primarySource` 份数最多（再看最新）的来源；`hints[]` 人话提示（`kind` ∈ `follower_jump` / `follower_drop` / `engagement_outlier` / `followers_up_engagement_down`，`params`，`messages` 三语），只供人看，不参与排序。不存在 404。选人端 / 运营端详情页可直接调用 `creatorTrends(env, id, query)`（`apps/api/src/ingest/trends.ts`） |
| GET | `/api/ingest/name-search` | ingest.read | 中韩昵称模糊搜索：`?q=`（最多 100 字）、`limit=`（默认 20，≤100）、`status=`（逗号分隔）。`items[]`：`id`、`displayName`、`xhsId`、`status`、`followers`、`exact`（去掉空白标点大小写后昵称里直接含有 q）、`score`（q 的片段有多少出现在昵称里，0–1）。先按 `exact`、再按 `score`、再按粉丝排；`score` < 0.5 且不含 q 的不返回。博主池等其他列表用 `nameMatchSql(alias, '$n')`（`apps/api/src/ingest/name-search.ts`）拿到同一套 `where` / `order` 片段，走同一个索引 |
| GET | `/api/ingest/raw/:creatorId` | ingest.read | 该博主的每次抓取与当次原文（内容相同的只存一份，这里照样逐条还原），新的在前：`{ ...最新一条, items: [...] }`（顶层字段兼容旧调用方），每条带 `contentHash`（相同即原文完全一样），`?limit=` 默认 30、最多 100 |

## 账号管理（工作端源站）

不在 API 上，而是在任一工作端源站的 Nitro 路由（`libs/panel/server/api/kcs-admin/*`），和登录同一处、直接读写 TinyShip 库。权限 `admin.users`，只有 platform_admin 有；每次请求都现查会话（`Authorization: Bearer` 或 better-auth cookie），不走缓存。错误体是 Nitro 形状：`{ statusCode, data: { error: <code> } }`。

| 方法 | 路径 | 说明 |
|---|---|---|
| GET | `/api/kcs-admin/users` | `{ items: [{ id, email, name, role, disabled, self, createdAt, lastLoginAt }] }`。`role` 是 KCS 角色，没分工的是 `null`；`lastLoginAt` = 该账号还留在库里的会话里最新一次的创建时间（主动退出会删掉那次会话，所以不计入） |
| POST | `/api/kcs-admin/users` | `{ email, password, role, name? }` → 201。邮箱格式不对 400 `invalid_email`；密码少于 8 位 400 `weak_password`；角色不在五角色里 400 `invalid_role`；邮箱已有账号 409 `email_taken`。名字不填用邮箱 @ 前的部分 |
| PATCH | `/api/kcs-admin/users/:id` | `{ role? , disabled? }`。停用 = better-auth `banned`，同时删掉该账号的全部会话，登录直接 403；恢复后可用原密码登录。不能改自己的角色或停用自己（409 `self`）；账号不存在 404 |

生效时间：工作端源站上立刻生效（旧会话 `get-session` 马上为空）；API 对会话有 10 秒正缓存（`SESSION_CACHE_MS`），所以**改角色和停用在 API 上最多 10 秒后生效**，这段时间里旧 token 仍按旧角色放行。黑盒 `14-accounts` 按 15 秒等待。

## 运维端

| 方法 | 路径 | 权限 | 说明 |
|---|---|---|---|
| GET | `/api/dev/health` | dev.read | DB、任务状态分布、搁置条数（`deadLetters`）、当前在抓的来源与持有者（`running`） |
| GET | `/api/dev/jobs` `/jobs/:id` | dev.read | 所有任务（抓取 + 导入），列表分页、按最近更新倒序 |
| POST | `/api/dev/jobs/:id/retry` | dev.retry | 同 ingest retry；会一并结掉该任务的搁置记录 |
| GET | `/api/dev/failures` | dev.read | 失败任务与错误摘要 |
| GET | `/api/dev/dead-letters` | dev.read | 搁置记录；`?state=open\|replayed\|dismissed\|all&kind=job\|record&source=`，未知取值 400 |
| GET | `/api/dev/dead-letters/:id` | dev.read | 单条，含原始 JSON（`record`）或参数 + 游标（`job`） |
| POST | `/api/dev/dead-letters/:id/replay` | dev.retry | `job`：按原参数从游标新建任务；`record`：用留下的 JSON 重读入库（不打平台）。仍失败 409 并记次数，满 3 次后拒绝 |
| POST | `/api/dev/dead-letters/:id/dismiss` | dev.retry | 标记不再处理；非待处理状态 409 |
| GET | `/api/dev/pipeline` | dev.read | 每个来源：今日调用 / 配额、速率、最近成功时间 |
| GET | `/api/dev/audit` | dev.read | 最近 100 条审计：`id`、`actorId`、`action`、`entityType`、`entityId`、`summary`、`createdAt` |
| GET | `/api/dev/i18n-theme` | dev.read | 三语键数与主题信息 |

## 响应形状速查

```ts
// 池行 / 分派行
QueryResultRow = QueryRow & { tier, cohort: { source, tier, size }, percentiles: { [key]: { percentile, band } }, flags: { key, tone }[] }

// 快照
MetricSnapshot = { id, creatorId, source, window, fetchedAt, jobId, metrics: CreatorMetrics }

// 来源链接
CreatorSourceLink = { source, externalId, firstSeenAt, lastSeenAt }

// 任务进度
IngestJobProgress = { status, pagesDone, cursor, quotaUsed, attempts, nextRunAt, error }
```
