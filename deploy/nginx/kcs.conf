map $http_upgrade $connection_upgrade {
    default upgrade;
    ''      close;
}

upstream kcs_marketing {
    server 127.0.0.1:7000;
}

upstream kcs_legacy {
    server 127.0.0.1:7001;
}

upstream kcs_ops {
    server 127.0.0.1:7002;
}

upstream kcs_dev {
    server 127.0.0.1:7003;
}

upstream kcs_select {
    server 127.0.0.1:7004;
}

upstream kcs_api {
    server 127.0.0.1:7100;
}

server {
    listen 80;
    server_name kcs.example.com;

    client_max_body_size 20m;

    location /api/ {
        proxy_pass http://kcs_api;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection $connection_upgrade;
    }

    location / {
        proxy_pass http://kcs_marketing;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection $connection_upgrade;
    }
}

server {
    listen 80;
    server_name ops.kcs.example.com;

    location / {
        proxy_pass http://kcs_ops;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection $connection_upgrade;
    }
}

server {
    listen 80;
    server_name dev.kcs.example.com;

    location / {
        proxy_pass http://kcs_dev;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection $connection_upgrade;
    }
}

server {
    listen 80;
    server_name select.kcs.example.com;

    location / {
        proxy_pass http://kcs_select;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection $connection_upgrade;
    }
}

server {
    listen 80;
    server_name legacy.kcs.example.com;

    location / {
        proxy_pass http://kcs_legacy;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection $connection_upgrade;
    }
}
