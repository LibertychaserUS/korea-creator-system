apiVersion: v1
kind: Service
metadata:
  name: api
  namespace: kcs
spec:
  ports:
    - port: 7100
  selector:
    app: kcs-api
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: kcs-api
  namespace: kcs
spec:
  replicas: 1
  selector:
    matchLabels:
      app: kcs-api
  template:
    metadata:
      labels:
        app: kcs-api
    spec:
      serviceAccountName: kcs-api
      containers:
        - name: api
          image: kcs-api:local
          ports:
            - containerPort: 7100
          env:
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: kcs-api
                  key: database-url
            - name: S3_ENDPOINT
              value: http://minio:9000
            - name: S3_BUCKET
              value: kcs-assets
            - name: S3_ACCESS_KEY
              valueFrom:
                secretKeyRef:
                  name: kcs-api
                  key: s3-access-key
            - name: S3_SECRET_KEY
              valueFrom:
                secretKeyRef:
                  name: kcs-api
                  key: s3-secret-key
            - name: S3_FORCE_PATH_STYLE
              value: "true"
            - name: WEB_ORIGIN
              value: https://kcs.example.com,https://legacy.kcs.example.com,https://ops.kcs.example.com,https://dev.kcs.example.com,https://select.kcs.example.com
          readinessProbe:
            httpGet:
              path: /api/health
              port: 7100
